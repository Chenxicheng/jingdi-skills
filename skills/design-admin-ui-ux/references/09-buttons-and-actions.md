# 按钮与操作

- 每个操作上下文只突出一个主操作；次要操作降低权重，低频操作进入更多菜单。
- 使用“动词 + 对象”命名，如“创建订单”“保存规则”“删除成员”。
- 操作强度匹配业务重要性，普通前进动作使用常规层级。
- 接受与拒绝等中立选择使用相近权重，让用户按真实意图选择。
- 图标按钮提供可访问名称和 Tooltip；含义不明确或风险较高时同时显示文字。
- Loading 状态保持按钮尺寸稳定并阻止重复提交；长任务同时提供过程反馈。
- 可逆操作优先直接执行并提供撤销；不可逆或高影响操作增加确认或安全校验。
- 提交、取消、返回和更多操作的位置在同类页面保持稳定。
